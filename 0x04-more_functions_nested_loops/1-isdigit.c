#include "main.h"

/**
 * _isdigit - A program that checks for a digit from 0 through 9
 *
 * @c: Number that is to be checked
 *
 * Return: 1 or 0
 *
 */
int _isdigit(int c)
{
	return (c >= '0' && c <= '9');
}
